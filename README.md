# Chat App with React, Socke.IO, Nodejs, MongoDB and Mongoose

## Install dependencies

In the project directory, you can run:

### `yarn`

## Run app

In the project directory, you can run:

### `yarn start`
